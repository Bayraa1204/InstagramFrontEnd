import { Settings } from "lucide-react";

const Setting = () => {
  return (
    <div className="fixed z-50 text-white" style={{ right: "4px", top: "4px" }}>
      <Settings />
    </div>
  );
};

export default Setting;
